`** arr[2][3];`
|연산자|순서|||
|:---:|:---:|:---:|:---:|
|* | *| arr[2]|[3];|
| 4 |3| 1| 2|


`void (*ID[10])(void);`  


그런데 배열은 불가능한게 있다.

문제. 보이드 보이드 함수 10개를 만들어보세요. 함수의 주소를 담는 배열

void ID[10](void);
* 이건 안된다.
* 내부에 테이블에 10개칸은 생겼는데 뭘 넣는지 정보가 하나도 없다.
* 함수 크기 있는가? 
* 함수도 주소를 가지고 있으니까 메모리 올라가니까 함수를 집어넣은게 아니라 함수의 주소를 담아놓는 것은 가능하니까. 그런데 주소만 넣으면 호출 못한다 틀에 정확히 맞춰야 한다. 


최적화 옵션 쓰면 크기가 바뀐다. 가변한다. 컴파일러마다 다르다. 함수가 